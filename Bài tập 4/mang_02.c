
#include <stdio.h>
int main(){
	int a;
	printf("Nhap a = ");
	scanf("%d", &n);
  
  int b[n];
	for(int c = 0;c < n; c++){
    printf("Nhap phan tu thu %d = ", c + 1);
	scanf("%d", &a[i]);
  }
   printf("mang a: ");
  for(int d = 0;d < n; d++){
    printf(" %d ", a[i]);
    
  }
  for(int i = 0;i < n; i++){
  if (a[i] % 2 == 0) {
      printf("\n%d la so chan\n", a[i]);
  }
  }
     return 0;}
